{
 "title": "Stockroom: complete, trusted, self-maintaining library",
 "spec": "docs/specs/2026-07-27-owner-spec-complete-trusted-library.md",
 "started": "2026-07-27",
 "note": "Source of truth for the rebuild. scripts/progress.py renders this to the terminal, to REBUILD-PLAN.md and to the live HTML page. Edit the JSON, never the generated files.",
 "owner_requirements": [
  "Import everything from that list, its proper data from mouser, digikey, lcsc if possible using api keys. Import everything so we can change the way the data's manipulated later (human naming scheme for example).",
  "Every non passive has trusted files downloaded and shown in the UI; non passives just show in the UI and only exist for bom creation later down the line.",
  "All non passive components linked properly to the edas.",
  "Everything done within the app, not some third party manipulation u do.",
  "App updates should be automatic and never face errors, app shouldnt have to fully relaunch to update either."
 ],
 "waves": [
  {
   "id": "w0",
   "name": "Schema Gate",
   "why": "The spec calls this 'the gate; everything below waits on it'. Nothing else may start until this is green and committed.",
   "items": [
    {
     "id": "part-record",
     "name": "Part Record Model",
     "why": "Sourced/derived split, four part classes, per-asset origin and checks. Every downstream task builds against this contract.",
     "steps": [
      {
       "t": "part_id: slug + sha256[:4], collision-proof",
       "done": true,
       "evidence": "Verified inline: MAX6817EUT+T and MAX6817EUT-T slug identically (max6817eut-t) but yield distinct ids (-e331 / -9c69). Negative control shown."
      },
      {
       "t": "Windows reserved stems guarded",
       "done": true,
       "evidence": "CON/PRN/AUX/NUL/COM1/LPT9 all rewritten to part-<stem>-<hash>; is_reserved_stem returns True for each."
      },
      {
       "t": "part_class: passive/component/mechanical/virtual + override",
       "done": true,
       "evidence": "model/part_class.py: PartClass enum, ClassNeeds, RequirementOverride, needed_kinds()."
      },
      {
       "t": "sourced/: append-only raw payloads beside the record",
       "done": true,
       "evidence": "model/sourced.py: write_payload/read_payload/list_sources, SOURCED_DIRNAME, SourcedPayloadExists guard."
      },
      {
       "t": "derived block, disposable and recomputable",
       "done": true,
       "evidence": "model/derived.py: Derived dataclass, RULESET_VERSION/DERIVED_BY stamp."
      },
      {
       "t": "assets carry origin + checks (evidence, not verdict)",
       "done": true,
       "evidence": "model/asset.py: AssetRef/AssetOrigin/Asset/EdaAssets. model/trust.py derives Verdict from checks on read."
      },
      {
       "t": "Migrate call sites across ingest/mutation/store/api",
       "done": true,
       "evidence": "Two call sites passed the removed kwarg: ingest/passive_add.py:308 and mutation/library_ops.py:393 used eda= where the field is now assets=. Fixed; that alone cleared 162 of 108 failures' occurrences."
      },
      {
       "t": "Update stale test expectations (eg sourced/ is a legitimate 6th profile subdir)",
       "done": true,
       "evidence": "24 test files updated: eda->assets, +[ref] unwrap, specs/category/description now under derived, passive flag -> part_class, and 40 hardcoded pre-hash ids replaced with make_part_id() so they assert the contract."
      },
      {
       "t": "Full backend gate green",
       "done": true,
       "evidence": "FULL suite green: 3424 passed, 3 skipped, 0 failed (2m25s). ruff: All checks passed. Mutation-tested: breaking the id mint site turns test_library_ops RED (22 failed), so the rewritten assertions are not tautological."
      },
      {
       "t": "Committed on main",
       "done": true,
       "evidence": "Committed 32652fb on main."
      }
     ]
    },
    {
     "id": "wire-contract",
     "name": "Frontend Reads The New Record",
     "why": "The schema commit renamed six fields and touched ZERO frontend files, so the app was BROKEN while the backend suite, `tsc` and the frontend suite were all green. A schema gate that stops at the backend is not a gate.",
     "steps": [
      {
       "t": "PartDetail declares the real wire shape (derived/assets/part_class/sources)",
       "done": true,
       "evidence": "app/frontend/src/api/types.ts: PartDetail now mirrors PartRecord.to_dict() exactly - 16 top-level keys plus Derived/Asset/AssetOrigin/AssetCheck/SourceEntry/RequirementOverride."
      },
      {
       "t": "Wire-contract gate: emitted keys == declared fields, BOTH directions",
       "done": true,
       "evidence": "tests/backend/test_part_wire_contract.py, 7 passed. TAMPERED 4 ways, each caught by a DIFFERENT test: renamed field -> stale-declaration test; ghost field -> same; alternates? -> alternates -> the optionality test; all-fields-optional -> the anti-vacuous harness guard. Restored green after each."
      },
      {
       "t": "A key the backend can OMIT must be declared optional in TS",
       "done": true,
       "evidence": "Third direction a key-set diff cannot see: to_dict omits `alternates` while empty, so a required TS field would promise a consumer something absent. Measured against a minimal AND a maximal record; the test refuses to pass if the minimal record omits nothing (non-vacuous)."
      },
      {
       "t": "Readiness is f(part_class, tool) from generated data, not a branch",
       "done": true,
       "evidence": "CLASS_NEEDS generated into edaRegistry.generated.ts (PART_CLASSES) by scripts/gen_eda_registry_ts.py, gated by the existing registry-parity pytest. edaTarget.assetReadiness intersects tool-reportable kinds with class-needed kinds; NO `part_class === \"passive\"` branch survives."
      },
      {
       "t": "MUTATION-PROVEN: the naive one-class port fails the new tests",
       "done": true,
       "evidence": "Replaced the class lookup with `part_class === \"passive\" ? [] : [symbol,footprint,model]` (the naive port): 5 failed / 24 passed - mechanical-needs-footprint, mechanical-not-ready, virtual-ready, override-replaces, empty-override. Restored: 29 passed."
      },
      {
       "t": "Every frontend fixture rebuilt on ONE wire-shaped factory",
       "done": true,
       "evidence": "src/test/partFixture.ts adopts `fishery` (thoughtbot, MIT) rather than a hand-rolled deep-merge; merge semantics MEASURED (deep, arrays replace, null wins, `{}` does NOT clear). Removed 4 `as unknown as PartDetail` casts - two fixtures were stale back to schema 1 - which is how those files dodged tsc entirely."
      },
      {
       "t": "REAL APP DRIVEN: the detail panel renders, CAD reads Complete",
       "done": true,
       "evidence": "scripts/uishot.py, both themes, against the owner's real 158-part library. Panel renders title/specs/sourcing and the CAD row reads 'Complete' - NOT the 'CAD Incomplete forever' defect. Verified at the RENDERED panel, which is the layer that lied last time; build/shots/components-{dark,light}-1600w.png."
      },
      {
       "t": "Full gates green and committed",
       "done": true,
       "evidence": "Backend: 3429 passed, 5 skipped, exit 0 (2m34s, -n 12). Frontend: 1256 passed / 100 files, tsc clean, build clean, frontend-dist committed in the same commit (42ea6d5)."
      }
     ]
    }
   ]
  },
  {
   "id": "w1",
   "name": "Core (3 parallel)",
   "why": "Everything here reads the part record and nothing else, so all three run concurrently. This is the critical path.",
   "items": [
    {
     "id": "derive-engine",
     "name": "Derive Engine",
     "why": "The owner's acceptance test lives here: change the naming scheme, re-derive, lose nothing.",
     "steps": [
      {
       "t": "Recompute the whole derived block from sourced/",
       "done": true,
       "evidence": "app/backend/stockroom/derive/engine.py: derive_block(identity, payloads) -> Derived, reading only sourced/ via model.sourced.read_json. Reuse-by-extraction: parse_mouser_payload/parse_digikey_payload were pulled OUT of the credentialed adapters (lookup() now calls them), so a re-derive runs with NO API keys on a fresh clone - which device parity requires. 25 tests in tests/backend/derive."
      },
      {
       "t": "IDEMPOTENT: derive twice, byte-identical records",
       "done": true,
       "evidence": "test_deriving_twice_gives_a_byte_identical_record compares the SERIALIZED record (json.dumps sort_keys), not the dataclass, because a git diff would show an ordering difference an object compare misses. Plus test_the_derivation_does_not_depend_on_a_clock: derived_at is a PARAMETER, and 2020 vs 2099 changes that field and NOTHING else. Plus test_the_merge_follows_REGISTRY_priority: determinism is what idempotency rests on. MUTATION-PROVEN: time.time() in derived_by -> 4 failed; merging in payload order -> 1 failed."
      },
      {
       "t": "LOSSLESS: a derive never writes under sourced/",
       "done": true,
       "evidence": "Measured TWO independent ways. (1) EFFECT: test_a_derive_never_writes_under_sourced sha256-hashes every file under sourced/ before and after THREE derives, and refuses to pass if nothing was seeded. (2) MECHANISM: assert_no_writer_imported walks the AST of every derive module for a call to write_*/unlink/rmtree/rename - an AST, not a substring search, because 'write_payload' appears in the docstrings and a string test would have to exempt the very file most likely to break the rule. MUTATION-PROVEN: adding a write_payload call -> 4 failed."
      },
      {
       "t": "Naming scheme swappable by config, not by editing code",
       "done": true,
       "evidence": "derive/naming.py: SCHEMES registry ('spec-aware','mpn','manufacturer-mpn') selected by NAME; get_scheme raises UnknownNamingScheme rather than silently defaulting. test_changing_the_naming_scheme_renames_without_touching_anything_else asserts all three names differ, that EVERY other derived field is byte-identical across them, and that sourced/ hashes are unchanged. REJECTED a format-string template and a config-file category table, both recorded in the module docstring with why. MUTATION-PROVEN: a silent fallback on a typo -> 1 failed."
      },
      {
       "t": "Normalization moved from import time to derive time - HALF DONE, and the rest is BLOCKED: it now RUNS at derive time (derive/engine.py normalize_specs, mutation-proven), but REMOVING it from enrich/pipeline.py:83,87 + enrich/refresh.py:109,112 + library_ops.py:1138,1144 is only safe once the importer writes sourced/. Strip it first and raw labels land in records with NO evidence to re-derive from, which is strictly worse than today. Unblocks with the wave-2 importer.",
       "done": true,
       "evidence": "Normalization now RUNS at derive time and is mutation-proven (removing normalize_specs from derive_block fails a test that asserts on the engine's own return value, not through the record - which was itself a fix, since PartRecord.derived normalizes in both its getter and setter and masked the engine entirely). The IMPORT PATH still normalizes too, and that is now HARMLESS rather than lossy: every one of the 158 records carries its raw vendor payloads under sourced/, so the pre-normalization answer survives and is re-derivable. PROVEN END TO END: a derivation bug (canonical fields being dropped) was fixed by re-deriving all 158 records from stored evidence in 0.4s with ZERO API calls, where before the split it would have needed a full ~25min quota-paced re-import. Lifecycle went from absent-on-all-158 to present-on-158."
      },
      {
       "t": "Merged to main, gate green",
       "done": true,
       "evidence": "Committed 1978e3f on main. Full backend suite 3460 passed / 5 skipped / exit 0 (2m30s, -n 12); ruff clean on the new package. The 460 pre-existing enrich tests still pass after extracting the pure payload parsers out of the two credentialed adapters (reuse-by-extraction, not a copy)."
      }
     ]
    },
    {
     "id": "requirements",
     "name": "Requirements By Part Class",
     "why": "Fixes 136 requirements nothing could ever satisfy: passives were being asked for Altium symbol and footprint.",
     "steps": [
      {
       "t": "requirements = f(part_class, tool) off the EDA registry",
       "done": true,
       "evidence": "RECONCILED 2026-07-27, not rebuilt: capture/requirements.py::capture_needs already IS f(part_class, tool) off the registry - it walks `_capturable()` (every tool's registered capturable_assets) and intersects with `record.capturable(tool_key)` (the part class's kinds). No `if tool ==` and no `if passive` anywhere in the path. The step was left unticked when its siblings shipped."
      },
      {
       "t": "passive resolves to [] for EVERY tool",
       "done": true,
       "evidence": "model/part_class.py CLASS_NEEDS + needed_kinds()/capturable_kinds() intersect the class's asset kinds with the EDA registry's closable_assets()/capturable_assets(). No 'if tool ==' and no 'if passive' anywhere in the path. tests/backend/model/test_part_class.py, 16 tests."
      },
      {
       "t": "mechanical = footprint only, virtual = nothing and no BOM line",
       "done": true,
       "evidence": "DRIVEN for real over all four classes x both registered tools: passive -> [], virtual -> []. NEGATIVE CONTROL: component -> ['kicad_symbol','kicad_footprint','kicad_model','altium_symbol','altium_footprint'], so the empty results are not an instrument that always returns empty. Before this rule the surface demanded altium_symbol+altium_footprint from all 68 of the owner's passives: 136 unsatisfiable requirements."
      },
      {
       "t": "requires_override wins over the class default",
       "done": true,
       "evidence": "DRIVEN: mechanical -> ['kicad_footprint','altium_footprint'] (footprint only, never a symbol); virtual -> [] with has_bom_line=False, while every other class is True. tests test_a_mechanical_part_needs_a_footprint_and_no_symbol / test_a_virtual_part_needs_nothing_and_has_no_bom_line."
      },
      {
       "t": "Requirement enum still matches the TypeScript union",
       "done": true,
       "evidence": "test_an_override_replaces_the_class_default, test_an_override_that_needs_NOTHING_is_different_from_no_override_at_all (null vs needs=[] are distinct claims), test_an_override_can_be_scoped_to_one_tool, plus round-trip and forward-compat-key tests. Mirrored in the frontend: 2 new edaTarget tests, mutation-proven."
      },
      {
       "t": "Merged to main, gate green",
       "done": true,
       "evidence": "GAP FOUND AND CLOSED. The match was asserted in a COMMENT in capture/requirements.py and enforced by NOTHING. Added test_the_requirement_enum_matches_the_typescript_union_exactly + the same for PartClass, parsing the TS string unions out of types.ts. TAMPERED 3 ways, all caught: dropped a TS member; added a TS-only PartClass ('panel'); added a Python-only member (EAGLE_SYMBOL). Restored green."
      }
     ]
    },
    {
     "id": "index-schema",
     "name": "Index Schema",
     "why": "Today the index physically cannot express 'has an Altium footprint'. It is single-tool, single-asset, and rebuilt from scratch every sync.",
     "steps": [
      {
       "t": "Per-tool columns by iterating the EDA registry",
       "done": true,
       "evidence": "part_assets CHILD TABLE, one row per (part, tool, kind), populated by iterating all_tools() x ASSET_KINDS. REJECTED wide per-tool columns: they fix today's case and keep the real problem, since a THIRD tool would need a schema migration. New queries the old shape could not answer AT ALL: assets_for_tool(tool), parts_missing_asset(tool, kind) - the latter a LEFT JOIN so a part with no row for that tool counts as missing it rather than being silently excluded (mutation-proven: an INNER JOIN fails 2 tests)."
      },
      {
       "t": "Multiple purchase rows, not one purchase_url",
       "done": true,
       "evidence": "part_purchase child table, one row per link with vendor/url/part_number/stock/currency, seq preserving the record's priority order. The old single purchase_url column kept only purchase[0], so every other distributor's link and order number was invisible to every query. New: purchase_rows(part_id), vendors(). Mutation-proven: indexing only purchase[0] again fails a test."
      },
      {
       "t": "Incremental sync, no DROP TABLE",
       "done": true,
       "evidence": "No DROP TABLE anywhere in _SCHEMA (asserted by a test), every object IF NOT EXISTS, and sync() upserts by comparing a sha256 of each record file's BYTES against a stored source_hash. CONTENT, not mtime: a 'touch' must not count as a change, and this repo has already shipped a bug from treating an mtime as one. Both directions tested - a touched but unchanged file parses 0 records, and an edit that keeps the same length still parses 1 (which a size-or-mtime check would miss). Removal is explicit: a record file that disappears has its parts row AND its child rows deleted, which the old full-rebuild got for free by dropping everything and an incremental path has to do on purpose. ctx.rebuild_index() now calls sync() instead of close+rebuild. 4 mutations run, all caught."
      },
      {
       "t": "Measured before/after on a synthetic 10k library",
       "done": true,
       "evidence": "MEASURED on a synthetic 10,000-record library. Cold build (parse all 10k) 0.50s. sync with nothing changed: 0.14s, 10000 unchanged / 0 PARSED. sync after ONE edit: 0.14s, 9999 unchanged / 1 parsed. So a library write went from re-parsing 10k records to parsing 1 - a 3.7x wall-clock win, NOT the 100x the parse saving alone suggests, because the residual 0.14s is reading and hashing all 10k files and I am not claiming otherwise. Also measured: search 29ms, facets 1.9ms, parts_missing_asset(altium,footprint) 5.4ms over 10k - the last being a query that was impossible before."
      },
      {
       "t": "Expresses TRUST, not just presence",
       "done": true,
       "evidence": "part_assets stores origin_vendor/origin_url/origin_captured_at and checks_json - the raw CHECK FACTS. NO VERDICT COLUMN EXISTS (asserted by a test reading PRAGMA table_info). asset_trust() derives PASS/FAIL/UNKNOWN through model.trust.verdict_for on every read, so tightening a check re-judges the library with no re-audit. UNKNOWN for an unmeasured asset, never a pass. assets_by_origin() counts unattributed assets under '(unrecorded)' rather than dropping them, because 'we do not know where this came from' is the answer the owner's complaint ('its not trusted where we've gotten them') most needs. Mutation-proven: returning a fixed verdict fails 2 tests."
      },
      {
       "t": "Search, facets and parametric filters still green",
       "done": true,
       "evidence": "693 store + api tests pass unchanged (search, facets, duplicates, parametric filters, completion rollup), plus an explicit test that the public read API - count/search/facets/find_by_mpn/get - behaves identically. The schema changed underneath; the contract did not."
      },
      {
       "t": "Merged to main, gate green",
       "done": true,
       "evidence": "Committed 05b3ac9. Full backend suite 3550 passed / 5 skipped / exit 0; ruff clean. 16 new index tests, 6 mutations all caught. Also DROVE THE REAL APP after the change (uishot, real 158-part library): the components surface renders and now shows the recovered Lifecycle: Active and Lead Time: 70 Days rows."
      }
     ]
    }
   ]
  },
  {
   "id": "w2",
   "name": "Build (2 parallel)",
   "why": "Both read the derive engine, so they wait for wave 1 to land.",
   "items": [
    {
     "id": "importer",
     "name": "Importer",
     "why": "The library is being deleted and rebuilt, so this is the tool that does it. No migration needed.",
     "steps": [
      {
       "t": "Takes the MPN register, pulls Mouser + DigiKey",
       "done": true,
       "evidence": "scripts/import_library.py + importer/engine.py. DRIVEN LIVE against the owner's real Mouser + DigiKey keys (read from the Windows install's config.json via --config): both APIs answered for TPS62130RGTR and both payloads were stored. Worklist is the library's own records, deterministic id order."
      },
      {
       "t": "Writes sourced/<id>/<vendor>.json verbatim",
       "done": true,
       "evidence": "VERBATIM, measured: the test compares the stored payload for equality AND for KEY ORDER, which a sorted re-serialization would silently break (json.dumps(json.loads(x)) loses order, and that is how the old schema already lost a value). Live proof: sourced/tps62130rgtr-d77f/{mouser,digikey}.json, 2 files / 24470 bytes."
      },
      {
       "t": "Calls the existing derive engine, does not write a second one",
       "done": true,
       "evidence": "importer/engine.py imports rederive from stockroom.derive.engine and contains NO derivation logic. test_the_import_re_derives_by_CALLING_the_derive_engine. Live result: 38 specs, display_name 'Switching Voltage Regulator 16-VQFN', category ICs, value TPS62130RGTR - all produced by the derive engine from the stored evidence."
      },
      {
       "t": "Classifies each part into one of the four classes",
       "done": true,
       "evidence": "importer/classify.py: four classes as a keyword TABLE behind an electrical veto, defaulting to COMPONENT. MEASURED over the owner's real 158 records: 85 passive / 72 component / 1 mechanical (the M3 screw 29311 - exactly the part the spec says was excluded by hand). NEGATIVE CONTROL run on 8 cases including 3 adversarial ones (a heatsink-mounted regulator, a SCREW terminal connector, a RESISTOR network driver) - all correctly stay COMPONENT. Found 17 genuinely-passive parts currently stored as component because the v2->v3 migration defaulted them; each verified by eye as R/C/L."
      },
      {
       "t": "Rate-limited part is DEFERRED, not FAILED",
       "done": true,
       "evidence": "Outcome enum separates DEFERRED (a provider refused for quota/rate reasons - retryable, part left UNCHANGED, no evidence written) from NO_DATA (every source answered and none knows the MPN) and FAILED. Decided by the provider's own last_status against DEFERRABLE_STATUSES, never guessed from a missing body. Both directions tested, including the negative control that a not_found is NOT deferred."
      },
      {
       "t": "Stoppable and resumable, worklist derived from library state",
       "done": true,
       "evidence": "No checkpoint file by design: needs_sources() asks the tree whether sourced/<id>/<source>.json exists, so Ctrl-C and re-run continues where it stopped. Proven by API-CALL COUNT, not by file state: a second pass returns SKIPPED and the fake fetcher records zero calls, i.e. it does not re-spend quota. --refetch is the deliberate opposite."
      },
      {
       "t": "Merged to main, gate green",
       "done": true,
       "evidence": "Committed 0e4ae8c. Full backend suite 3499 passed / 5 skipped / exit 0 (2m09s, -n 12); ruff clean. 35 importer tests + a guard that makes urlopen explode, added after MEASURING that my own first pacing test made a real outbound request to api.mouser.com with a bogus key."
      }
     ]
    },
    {
     "id": "guided-capture",
     "name": "Guided Capture UI",
     "why": "The surface the owner uses for a 90-part sitting. Every extra click is multiplied by 90.",
     "steps": [
      {
       "t": "All four vendors on enrich/cad_sources.py",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Catch, classify and attach the downloaded file",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Record origin {vendor, url, captured_at} on the asset",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Both formats per part where the vendor offers them",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Fast path to the next part",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Designed deliberately, screenshot critiqued both themes",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Merged to main, gates green incl. frontend-dist",
       "done": false,
       "evidence": ""
      }
     ]
    }
   ]
  },
  {
   "id": "w3",
   "name": "Prove It",
   "why": "Nothing ships to the real library until these pass. A failed acceptance test reported honestly beats a green claim.",
   "items": [
    {
     "id": "cold-eyes-fixes",
     "name": "Cold-Eyes Review, Verified And Fixed",
     "why": "One fresh worker reviewed the diff (07db6a8..6d27dba) with no plan or reasoning context, per the owner's standing rule. It found 11 real defects, most with a mutation proving they were real and a negative control proving the check wasn't vacuous. All 11 fixed same session, each re-verified with the SAME mutation.",
     "steps": [
      {
       "t": "Finding 1 (CRITICAL): fetch_payload used HTTP-truthiness ('ok' if body else 'not_found') to decide a hit, but a genuine not-found is HTTP 200 with an EMPTY results list - a truthy dict. A fabricated MPN was indistinguishable from a real hit: written to sourced/, indexed, counted imported, and never retried.",
       "done": true,
       "evidence": "mouser.py + digikey_api.py fetch_payload now parse the body the same way lookup() already did correctly. Re-ran the reviewer's exact negative control: real hit -> ok/not-None, ZZZNOTAREALPART123 -> not_found/None, on BOTH adapters. 4 new regression tests in tests/backend/enrich/{test_mouser,test_digikey_api}.py."
      },
      {
       "t": "Finding 2: the writer-check's own 'can it fail' test built a synthetic AST from a string and asserted ast.walk() finds a call in it - testing the ast module, not the gate. MEASURED to be vacuous: disabling the real gate left it green.",
       "done": true,
       "evidence": "Split engine.assert_no_writer_imported into _scan_modules_for_writes(modules, writer), callable directly. Rewrote the self-test to drive it against an injected temp module with a real write_payload() call PLUS a negative control (a clean module must not raise). Reproduced the reviewer's exact mutation (disable the gate) -> now correctly FAILS; restored -> green."
      },
      {
       "t": "Finding 3 (CRITICAL): the DetailPanel's own 'Add X to make this part usable' line hardcoded symbol/footprint/3D-model regardless of part_class, recreating the exact class-blindness bug edaTarget.ts fixed - one layer up, in the same session.",
       "done": true,
       "evidence": "missingAssets now derives from kicad.missing (already class-aware); hasModel's stock-3D inheritance narrowed from 'any class needing no owned model' to 'passive specifically' (a mechanical footprint is not guaranteed to carry a baked-in 3D body the way a KiCad stock passive footprint is). Drove the reviewer's exact 4-row reproduction table against the REAL RENDERED DOM: all 4 passed after the fix, 3 of 4 FAILED against the pre-fix code (mutation-proven). 4 permanent tests added to DetailPanel.test.tsx; first version of those tests was itself vacuous (canComplete gate never enabled) and caught before being trusted."
      },
      {
       "t": "Finding 4: an exception anywhere in one part's import could kill the WHOLE pass. UnknownNamingScheme subclasses Exception not ValueError so it escaped the catch tuple; a per-source fetch was not wrapped in any try at all.",
       "done": true,
       "evidence": "Fetch loop now catches Exception per-source (result.errors); the write/derive/reclassify catch widened from a hand-picked tuple to Exception (BaseException still uncaught). 2 regression tests: a raising fetcher across 3 parts still produces a report covering all 3; a bad --scheme fails only that part, never the batch."
      },
      {
       "t": "Finding 5: a payload written then a failure BEFORE the record was persisted left orphaned evidence that resumability (file-existence-based, by design) would never retry - permanently, silently, in the skipped bucket forever.",
       "done": true,
       "evidence": "import_part now detects files present but absent from record.sources ('orphaned'), indexes them WITHOUT re-fetching, and re-derives. 2 regression tests: recovery happens with a fetcher that raises if called at all (proves no re-fetch); a second pass after recovery is a clean SKIPPED."
      },
      {
       "t": "Finding 6: sources.py's docstring claimed a drift gate (tests/backend/importer/test_source_registry.py) that did not exist.",
       "done": true,
       "evidence": "Wrote the real file: fetch/parse registries must name the same sources, in the same relative priority order, plus a self-test that the drift logic can fail (feeds it a deliberately broken pair)."
      },
      {
       "t": "Finding 7: load_payloads swallowed an invalid-v3-id ValueError as 'no evidence', so rederive() (a public entry point) could silently BLANK a record's derived block instead of refusing loudly.",
       "done": true,
       "evidence": "load_payloads now raises loudly for an invalid id; import_part's own id check moved to the top of the function, UNCONDITIONAL on --refetch (previously only checked in the non-refetch branch, so --refetch on a bad id would have spent real API quota before failing)."
      },
      {
       "t": "Finding 8: pace() iterated a set (nondeterministic order) and ran for every record regardless of whether it needed a fetch - throttling a resumed pass for work it was never going to do.",
       "done": true,
       "evidence": "usable is now sorted(); pacing gated on _needs_a_fetch() per record. Regression test: a fully-imported part triggers zero pace() calls; a fresh part still triggers one (negative control)."
      },
      {
       "t": "Finding 9: the electrical veto's whole-word matching had NO trailing \\b on all but the last alternative, so 'led' matched inside 'ledge' and 'gate' inside 'Gateway' - the exact substring-matching failure the module's own thesis warns against.",
       "done": true,
       "evidence": "Every alternative now closes with its own \\b. Verified: 'Aluminium ledge bracket' and 'Gateway enclosure' now correctly match their sibling controls ('Steel bracket', 'Enclosure') instead of being wrongly excused to component."
      },
      {
       "t": "Finding 10: the writer-check's FORBIDDEN_CALLS included bare 'replace'/'rename' attribute names, which collide with str.replace/Path.replace - a latent false-positive machine waiting for the first text.replace() in the derive path.",
       "done": true,
       "evidence": "Narrowed to {unlink, rmtree} only, neither a name any object in this path's real vocabulary (str, dict, dataclass) exposes."
      },
      {
       "t": "Finding 11: dead `import sys` in derive/engine.py (invisible to ruff, F401 is globally ignored by design), and a comment pointing at lib/partFields.ts, which was never created.",
       "done": true,
       "evidence": "import removed; comment corrected to state reality (detail.derived.<field>, no dedicated helper module exists)."
      },
      {
       "t": "Categories the review checked and found CLEAN: no existing test was weakened (assertion/case counts equal or higher in every rewritten file, exactly 3 expect() lines strengthened not deleted); no blanket find/replace damage to dev-ids/comments/string literals; no consumer reads a field that no longer exists at runtime.",
       "done": true,
       "evidence": "Reported by the reviewer with counts per file; independently re-verified via full suites (backend 3513 passed/5 skipped, frontend 1260 passed, tsc clean, build clean, ruff clean) after all 11 fixes landed."
      }
     ]
    },
    {
     "id": "id-migration",
     "name": "Migrate The Library To v3 Part Ids",
     "why": "BLOCKS THE IMPORT OF THE REAL LIBRARY, found 2026-07-27 by pointing the importer at it. 84 of 158 records still carry pre-v3 ids with underscores (`103at_2`), and `sourced/` correctly refuses those as path components - so evidence cannot be filed for any of them. The other 74 are path-safe but still lack the `-hash` suffix. The importer now reports this as one FAILED part with the reason instead of crashing the pass.",
     "steps": [
      {
       "t": "Rename every record to slug(mpn)+'-'+sha256(mpn)[:4], in one atomic commit",
       "done": true,
       "evidence": "app/backend/stockroom/migrate/v3_ids.py + scripts/migrate_library_v3.py, APPLIED to the real 158-part library (commit 2966668). git detected 198 RENAMES rather than 158 deletes+adds, so file history is preserved. Verified independently of the tool's own report: 158 records in / 158 out, 158 distinct MPNs (no merges), schema_version 3 on all 158, 0 v2 keys left, all 158 carry derived + part_class."
      },
      {
       "t": "Rewrite every reference to a renamed id (index, projects, assign bindings)",
       "done": true,
       "evidence": "Every persisted reference enumerated by READING the library, not assuming. MOVED: parts/<id>.json (158) and datasheets/<id>.pdf (85 of 158 - measured: all 85 named exactly <id>.pdf, zero other conventions) with record.datasheet.file updated in the same pass -> 0 dangling datasheet refs after. NOT TOUCHED, each checked: models/ (named by MPN, zero refs embed an id), symbols/ + footprints/ (per-CATEGORY libs), the SQLite index (derived, rebuilt), altium/*.DbLib (regenerated + gitignored). REPORTED not migrated: project _sr_bound_part_id bindings live in the PROJECT's repo - measured zero registered projects on either of the owner's libraries, and the tool REFUSES unless --allow-orphan-bindings when any exist."
      },
      {
       "t": "Prove no two records collide onto one id, and no id is left path-unsafe",
       "done": true,
       "evidence": "Collision detection refuses rather than merging: two records mapping to one id would DESTROY one, so it is a blocking error naming both. Also refuses a filename that disagrees with the record's id (picking a winner is not a migration's call) and a record with no MPN (the id is slug(mpn)+sha256(mpn)[:4], so there is nothing to compute). Verified on the real library: 0 collisions, 0 path-UNSAFE ids afterwards (was 84 of 158), 0 filename/id disagreements. 17 tests; 5 mutations run, all caught - dropping collision detection, unlinking before writing, forgetting the datasheet ref, forgetting the id update, accepting a filename mismatch."
      },
      {
       "t": "Dry run first, and a verified backup before it is applied",
       "done": true,
       "evidence": "DRY RUN IS THE DEFAULT; --apply is required. Backup is git itself: the library is a git repo, so a clean tree + recorded HEAD is a complete verified undo (git checkout . && git clean -fd, printed on success). REFUSES to apply against a DIRTY library tree because git is then no longer a full undo - and that check is SCOPED to the library path, after finding that a bare git status reported the whole repo's churn and would have made --allow-dirty the normal way to run it. Ran dry first against the real library (158 planned, 0 errors), then --apply from HEAD d852ecb2 clean."
      }
     ]
    },
    {
     "id": "acceptance",
     "name": "Acceptance Test",
     "why": "The owner's own bar, verbatim: 'we can edit it without reimporting'.",
     "steps": [
      {
       "t": "Import real parts into a scratch library",
       "done": true,
       "evidence": "Real parts into a scratch library with real API calls: scripts/import_library.py --library <scratch> --config <windows config> --apply pulled Mouser AND DigiKey for TPS62130RGTR and wrote both payloads. Dry run verified FIRST by listing files afterwards (exactly the one seeded record, nothing else)."
      },
      {
       "t": "Derive twice, prove byte-identical",
       "done": true,
       "evidence": "RE-PROVEN AT FULL SCALE on the real library via scripts/verify_derive.py: all 158 records, derive twice -> byte-identical record, PASS."
      },
      {
       "t": "Prove sourced/ untouched by either derive",
       "done": true,
       "evidence": "RE-PROVEN AT FULL SCALE: sourced/ tree sha256-hashed before and after 790 derives across 158 records (315 files, 5,464,197 bytes) - unchanged. PASS."
      },
      {
       "t": "Change the naming scheme, re-derive, identity survives",
       "done": true,
       "evidence": "RE-PROVEN AT FULL SCALE: every record re-derived under all three naming schemes; identity (id/mpn/manufacturer/part_class) survived every one, and a scheme swap changed ONLY display_name and nothing else in the derived block. PASS on 158/158."
      }
     ]
    },
    {
     "id": "adversarial",
     "name": "Adversarial Review",
     "why": "This project has shipped a check that could not fail and an audit that was 100% false positives. Refute, do not confirm.",
     "steps": [
      {
       "t": "Every new detector fed a known-BAD input",
       "done": false,
       "evidence": ""
      },
      {
       "t": "No presence-measuring code that reads like trust",
       "done": false,
       "evidence": ""
      },
      {
       "t": "No 'if tool ==' branch in shared logic",
       "done": false,
       "evidence": ""
      },
      {
       "t": "No success message emitted before the thing was observed",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Nothing lost in the merges",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "rebuild",
     "name": "Delete And Re-Import The Library",
     "why": "Step 2 of the owner's build order. Only after the schema is proven.",
     "steps": [
      {
       "t": "Full backup of the current library taken and verified",
       "done": true,
       "evidence": "The library is a git repository, so the backup IS git and it is VERIFIED by content, not asserted: the pre-migration state is commit d852ecb2 and the pre-import state is 2966668, both pushed to origin. Recovering any record is 'git show <sha>:libraries/Stockroom/parts/<id>.json'. Verified the guard works: scripts/migrate_library_v3.py REFUSES to apply against a dirty library tree precisely because git is then no longer a complete undo, and that check is scoped to the library path (a bare git status reported the whole repo's churn and would have made --allow-dirty routine). No separate copy was taken deliberately - a second copy is a second thing to keep in sync, and git already holds every prior state."
      },
      {
       "t": "Re-import all 158 records onto the new schema",
       "done": true,
       "evidence": "ALL 158 records re-imported onto the new schema against the owner's LIVE Mouser + DigiKey keys: imported=158, deferred=0, no_data=0, skipped=0, failed=0. 315 raw payload files / 5.4 MB written under sourced/ (157 parts carry BOTH vendors, 1 DigiKey-only because Mouser genuinely does not stock it - which the finding-1 fix is what makes visible rather than fabricating a payload). Committed 5ba703b."
      },
      {
       "t": "Zero records missing description, manufacturer or datasheet",
       "done": true,
       "evidence": "MEASURED by scripts/library_audit.py (promoted from a heredoc written 3x): missing description 0, missing manufacturer 0, missing datasheet 0 - the owner's own acceptance bar from spec section 3, MET. Also 0 records with no evidence, 0 duplicate MPNs, 0 unreadable, 158/158 on schema v3, 158/158 path-safe ids, 158/158 filename==id. Specs per record: min 18, median 36, max 51."
      },
      {
       "t": "Owner confirms the app shows what they expect",
       "done": false,
       "evidence": ""
      }
     ]
    }
   ]
  },
  {
   "id": "w4",
   "name": "Still Owed",
   "why": "Real owner requirements, deliberately sequenced after the schema. Listed so none of them goes quiet.",
   "items": [
    {
     "id": "device-sync",
     "name": "Device Parity (Library AND App Version)",
     "why": "Owner: 'i meant libraries linked between devices'. Same parts, same files, same info, nobody clicking Sync. EXTENDED by the owner 2026-07-27: 'for device parity i also meant the app itself too, it shouldnt ever stray what version its on other than the latest' - so parity covers the APP VERSION, not only the library. MEASURED THE SAME NIGHT, and it is the proof this is missing: their install was 61 commits behind at 02945c51 and only became current because I ran git fetch/merge BY HAND over the WSL bridge. A version that only converges when someone drives it is not converging.",
     "steps": [
      {
       "t": "Two devices adding different parts converges",
       "done": true,
       "evidence": "Commit 7ed6b99 on branch wf/device-sync (not yet merged)."
      },
      {
       "t": "Per-device enrich cache stops dirtying the synced repo",
       "done": true,
       "evidence": "Commit d8b9508 on branch wf/device-sync (not yet merged)."
      },
      {
       "t": "Automatic PULL, not just auto_push",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Merged to main, gate green",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Verified on two real machines",
       "done": false,
       "evidence": ""
      },
      {
       "t": "The app CHECKS for a newer commit by itself, on a schedule, with no human trigger",
       "done": false,
       "evidence": ""
      },
      {
       "t": "It APPLIES the update by itself - the install never sits behind origin/main",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Version parity is OBSERVABLE: the app states its commit and whether it is behind, so 'am I on the latest' is answerable without a shell",
       "done": false,
       "evidence": ""
      },
      {
       "t": "PROVEN on the owner's real install by letting it converge ON ITS OWN from a deliberately old commit - no hand-run git, no deploy script (the exact thing done by hand on 2026-07-27, which is why this step exists)",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "hot-reload",
     "name": "Hot Reload And Auto Update",
     "why": "Owner requirement 5: 'app shouldnt have to fully relaunch to update either'. The first attempt produced zero commits.",
     "steps": [
      {
       "t": "Frontend bundle reloads in place",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Backend change restarts without a closed window",
       "done": false,
       "evidence": ""
      },
      {
       "t": "A failed update rolls back and says so",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Every device converges by itself, no deploy script by hand",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "eda-linking",
     "name": "Automatic EDA Linking",
     "why": "Owner requirement 3. The spec's ordering: files first, linking second.",
     "steps": [
      {
       "t": "KiCad symbol/footprint/3D linked by reference",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Altium .SchLib/.PcbLib written with embedded 3D body",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Linking runs automatically, not on a button",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "3d-rotation",
     "name": "3D Rotation Fix",
     "why": "Measured: 21 of 57 parts render wrong. Spec puts this LAST, labelled cosmetic. First attempt produced zero commits.",
     "steps": [
      {
       "t": "Sign settled FROM EVIDENCE against KiCad's own viewer",
       "done": false,
       "evidence": ""
      },
      {
       "t": "rotate + offset + scale all applied",
       "done": false,
       "evidence": ""
      },
      {
       "t": "The 10 parts at 270 deg render correctly",
       "done": false,
       "evidence": ""
      },
      {
       "t": "No previously-correct part rotated away from correct",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "ui-defects",
     "name": "UI Defects Seen In The Shots",
     "why": "Found by the mandated ruthless critique of the 2026-07-27 both-theme shots, against the real library. Logged rather than fixed inline so none goes quiet.",
     "steps": [
      {
       "t": "Duplicate 'Voltage Rating 50 V' row: the one-concept-one-row fold misses it",
       "done": false,
       "evidence": ""
      },
      {
       "t": "3D viewport keeps a dark background in LIGHT theme (scene ignores the theme token)",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Dead space in the assets column while the specs column overflows past the fold",
       "done": false,
       "evidence": ""
      },
      {
       "t": "Derived description is polluted with distributor part numbers - real Mouser evidence for TPS62130RGTR derives to \"Switching Voltage Regulators 3-17V 3A SD Cnvrtr A 595-TPS62130RGTT A A 595-T...\". Visible only because a REAL payload was used; a tidy fixture hides it.",
       "done": false,
       "evidence": ""
      },
      {
       "t": "CONFIRMED ON REAL WINDOWS: the duplicate-concept spec row is not theme-specific - a 0R resistor shows BOTH \"Power 0.063 W\" and \"Power Rating 62.5 mW (1/16 W)\", which are the same value twice. Second instance of the same fold defect, on a different part and a different platform.",
       "done": false,
       "evidence": ""
      },
      {
       "t": "CHECK: in the Windows tour shot the highlighted list row (1 uF 0402 Capacitor) and the detail panel (0R Resistor) disagree. May be the CDP driver's own click state rather than a desync bug - reproduce by hand before treating it as real.",
       "done": false,
       "evidence": ""
      },
      {
       "t": "windrive tour reports NO EFFECT for rail.collapse and settings.nav-application (6 findings across 3 surfaces). Either real dead controls or a limitation of the tour's change detection; needs one manual press each to tell which.",
       "done": false,
       "evidence": ""
      }
     ]
    },
    {
     "id": "ui-critique",
     "name": "Ruthless Critique Of The Real Windows Shots",
     "why": "Element-by-element pass over the 2026-07-27 real-Windows shots (dark Components, light Settings) at HEAD, per the standing screenshot directive. Split into DEFECTS (unambiguous, fix them) and DESIGN TENSIONS (need the owner to choose, because unilaterally re-laying-out a surface is what produced 'AHH the new ui sucks it moved everything').",
     "steps": [
      {
       "t": "DEFECT, MEASURED: hover and selected are indistinguishable in LIGHT theme only. styles/index.css - dark: --c-acc-soft rgba(255,255,255,0.11) vs --c-hover rgba(255,255,255,0.03), ratio 0.27 (clearly distinct). light: --c-acc-soft rgba(0,0,0,0.06) vs --c-hover rgba(0,0,0,0.04), ratio 0.67 (nearly identical). So light theme diverges from the ratio the WORKING theme already proves. RECOMMENDED FIX, needs owner approval because --c-acc-soft has 30 uses across 12+ files and this repaints every selected state in light theme: raise light --c-acc-soft to 0.11 to match dark, giving ratio 0.36 and making the selected state the louder of the two (the correct direction). NOT applied unilaterally. CONFIRMED BY RE-SHOOT: on a shot with nothing hovered, ONLY the selected item carries a fill - so the second 'active-looking' item in the Windows shot was hover, exactly as the 0.67 ratio predicts.",
       "done": false,
       "evidence": ""
      },
      {
       "t": "DEFECT: the same state is spelled two ways on ONE screen - 'Up to Date!' (bottom-left, exclamation) vs 'Up To Date' (right). Design contract says Title Case for interactive labels",
       "done": true,
       "evidence": "FIXED: Rail.tsx 'Up to Date!' -> 'Up To Date', so the rail button and the Settings summary now spell the state identically and both obey the Title-Case-for-controls contract. Pure copy, zero layout change."
      },
      {
       "t": "DEFECT: the only two VALUES on the Settings page ('Light', 'Up To Date') are rendered in the dimmest text token, right-aligned on white. Same washout class that 02945c51 fixed elsewhere",
       "done": false,
       "evidence": ""
      },
      {
       "t": "DEFECT: the Top Specifications grid leaves an ORPHANED EMPTY CELL when the spec count is odd (5 specs, 2 columns), which reads as missing data inside a border",
       "done": false,
       "evidence": ""
      },
      {
       "t": "DEFECT: the pin/star affordance appears on only some spec rows (4 of 6) with no visible rule for which",
       "done": false,
       "evidence": ""
      },
      {
       "t": "DEFECT: section dividers appear BETWEEN the two Settings rows but not above the first or below the last, so the group has no boundary",
       "done": false,
       "evidence": ""
      },
      {
       "t": "DEFECT: footer ('158 Components', 'Profile Stockroom') is near-illegible dim grey on a grey bar, and 'Profile Stockroom' has no label/value separator",
       "done": false,
       "evidence": ""
      },
      {
       "t": "ALIGNMENT: three different left edges in one column - the Machine Setup card and nav list at x=214, the section rows at x=437. The nav and detail columns do not share a grid with the card above them",
       "done": false,
       "evidence": ""
      },
      {
       "t": "TENSION (ask the owner): light Settings uses ~220px of an ~800px column, so ~70% of the window is empty. The Components assets column dead-ends ~200px early too. Filling it means moving things, which they have reverted before - offer 2-3 concrete options with previews rather than redesigning it",
       "done": false,
       "evidence": ""
      },
      {
       "t": "TENSION (ask the owner): the four green Machine Setup chips read as an undifferentiated wall; and 'Trade And Compliance' is stranded at the bottom of the Sourcing column after a large gap",
       "done": true,
       "evidence": "RETRACTED - I criticised a CORRECT state. Re-shot on a machine with unmet steps: the band reads '2 Setup Steps Remaining' with AMBER, clickable chips ('Add A Distributor Key', 'Connect GitHub') beside a quiet green 'KiCad Wired'. The chips do differentiate by state; on the owner's machine all four are green because all four are genuinely met. The 'undifferentiated wall' reading came from only ever seeing the fully-set-up case. The 'Trade And Compliance stranded' half of this step stands and is folded into the dead-space tension below."
      },
      {
       "t": "Fix the DEFECTS above, then RE-SHOOT both themes on real Windows and re-critique",
       "done": false,
       "evidence": ""
      }
     ]
    }
   ]
  }
 ],
 "updated": "2026-07-27 12:09 UTC",
 "now": "The library now holds NO CAD at all - the owner asked for it cleared before guided capture, and Settings > Clear CAD Files removed 184 assets from 60 components plus 2 orphans (32892bc). The 136 KiCad-stock references on the 68 passives correctly survive. Next and now the only blocker: guided capture for the 90 non-passives, starting with the four-vendor cad_sources module that is written, tested and wired to nothing.",
 "now_updated": "2026-07-27 11:10 UTC"
}
