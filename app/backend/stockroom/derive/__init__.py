"""The derive engine: `sourced/` raw evidence + identity -> the recomputable `derived` block.

Read `engine.py` first; it holds the three properties the spec's acceptance test measures
(idempotent, lossless, disposable) and why each is a property of the code rather than a promise.
"""
