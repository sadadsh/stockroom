"""Library migrations: one-time, dry-run-first upgrades of an on-disk library.

See `v3_ids.py` for the v2 -> v3 schema write-back plus the decided part-id rename, and why the
library being a git repo is what makes a separate backup copy unnecessary.
"""
