// Injected by Vite's `define` (see vite.config.ts): the package version plus the app-repo git
// short SHA, resolved at build time and baked in as a constant. Declared here so TypeScript and
// the bundle both see the global.
declare const __APP_VERSION__: string;
